server:
  port: 9091
spring:
  application:
    name: config-server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
encrypt:
  key: secretkey
management:
  endpoints:
    web:
      exposure:
        include: "*"